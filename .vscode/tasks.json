{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build QEMU",
            "type": "shell",
            "command": "make",
            "args": [
                "-j",
                "24"
            ],
            "options": {
                "cwd": "${workspaceFolder}/qemu/build"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
